spring:
  h2:
    console:
      path: /h2-console
      enabled: true

  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password: admin
    url: jdbc:h2:mem:testdb

  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update